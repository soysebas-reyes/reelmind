{
  "name": "reelmind",
  "version": "0.1.0",
  "description": "An open-source, AI-native video editor for Windows. Independent derivative of palmier-io/palmier-pro (macOS).",
  "license": "GPL-3.0-or-later",
  "main": "./out/main/index.js",
  "author": "soysebas-reyes",
  "homepage": "https://github.com/soysebas-reyes/reelmind",
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "preview": "electron-vite preview",
    "start": "electron-vite preview",
    "fetch:ffmpeg": "node scripts/fetch-ffmpeg.mjs",
    "pack": "npm run fetch:ffmpeg && electron-vite build && electron-builder --dir",
    "dist": "npm run fetch:ffmpeg && electron-vite build && electron-builder",
    "typecheck:node": "tsc --noEmit -p tsconfig.node.json --composite false",
    "typecheck:web": "tsc --noEmit -p tsconfig.web.json --composite false",
    "typecheck": "npm run typecheck:node && npm run typecheck:web",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.105.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "electron-updater": "^6.8.9",
    "immer": "^11.1.8",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "zod": "^4.4.3",
    "zustand": "^5.0.14"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^5.2.0",
    "electron": "^42.4.1",
    "electron-builder": "^26.15.3",
    "electron-vite": "^5.0.0",
    "typescript": "^6.0.3",
    "vite": "^7.3.5",
    "vitest": "^3.2.6"
  }
}
